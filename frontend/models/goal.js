class Goal {
    constructor(
      id,
      profileId,
      title,
      duration,
      deadline
    ) {
      this.id = id;
      this.profileId = profileId;
      this.title = title;
      this.duration = duration;
      this.deadline = deadline;
    }
  }
  
export default Goal;
  