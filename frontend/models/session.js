class Session {
    constructor(id, duration, time, completed) {
        this.id = id;
        this.duration = duration;
        this.time = time;
        this.completed = completed;
    }
}

export default Session;