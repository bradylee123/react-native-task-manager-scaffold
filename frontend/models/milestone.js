class Milestone {
    constructor(
      id,
      goalId,
      title,
      duration,
      deadline
    ) {
      this.id = id;
      this.goalId = goalId;
      this.title = title;
      this.duration = duration;
      this.deadline = deadline;
    }
  }
  
export default Milestone;